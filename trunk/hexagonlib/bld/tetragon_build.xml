<?xml version="1.0" encoding="UTF-8"?>
<!--
*********************************************************************************************
	Buildfile for tetragon framework
	Nov 24, 2009
*********************************************************************************************
-->
<project name="tetragon framework" default="Init Project" basedir="..">
	
	<!-- application information -->
	<property name="app.name"			value="tetragon framework"/>
	<property name="app.version"		value="0.2.0"/>
	<property name="app.releasestage"	value="beta"/>
	<property name="app.copyright"	value="Copyright(c) Hexagon Star Softworks"/>
	
	<!-- version of the used flex sdk -->
	<property name="sdk.version"		value="3.4.0"/>
	
	<!-- version of the targetted flash player (9.0.124 or 10.0.12) -->
	<property name="fpv.version"		value="10.0.12"/>
	
	<!-- default project folders -->
	<property name="src"				value="src"/>
	<property name="swc"				value="swc"/>
	<property name="doc"				value="doc"/>
	
	<!-- eclipse workspace & additional source/swc pathes -->
	<property name="workspace"		value="${eclipse.workspace}"/>
	
	<!-- sdk pathes -->
	<property name="sdk"				value="${flex.sdk.basepath}/${sdk.version}"/>
	<property name="FLEX_HOME"		value="${sdk}"/>
	<property name="fcsh"				value="${sdk}/bin/fcsh.exe"/>
	<property name="mxmlc"			value="${sdk}/bin/mxmlc.exe"/>
	<property name="compc"			value="${sdk}/bin/compc.exe"/>
	<property name="asdoc"			value="${sdk}/bin/asdoc.exe"/>
	
	
	<!-- ==================================================================================== -->
	<!-- TARGETS                                                                              -->
	<!-- ==================================================================================== -->
	
	<target name="Init Project" description="Does nothing.">
	</target>
	
	
	<target name="swc" description="Generates the SWC of the tetragon framework.">
		<prepareBuild/>
		<echo message="Generating tetragon framework SWC ..." level="info"/>
		<flextasks.compc
			output="${basedir}/${swc}/tetragon.swc"
			target-player="${fpv.version}"
			debug="false"
			optimize="true"
		>
			<load-config filename="${FLEX_HOME}/frameworks/air-config.xml"/>
			<source-path path-element="${basedir}/${src}/tetragon/framework"/>
			<include-sources dir="${basedir}/${src}/tetragon/framework" includes="**/*.as"/>
		</flextasks.compc>
	</target>
	
	
	<target name="docs" description="Generates the docs for the tetragon framework.">
		<echo message="Generating tetragon framework Docs ..." level="info"/>
		<exec executable="${asdoc}" spawn="false">
			<arg value="-source-path"/>
			<arg path="${basedir}/${src}/tetragon/framework"/>
			<arg value="-doc-sources"/>
			<arg path="${basedir}/${src}/tetragon/framework"/>
			<arg value="-main-title"/>
			<arg path="tetragon framework API Documentation"/>
			<arg value="-window-title"/>
			<arg path="tetragon framework API Documentation"/>
			<arg value="-output"/>
			<arg path="${basedir}/${doc}/tetragon"/>
			<arg line="-target-player=10.0.0"/>
		</exec>
	</target>
	
	
	<!-- ==================================================================================== -->
	<!-- MACRODEFS                                                                            -->
	<!-- ==================================================================================== -->
	
	<macrodef name="prepareBuild">
		<sequential>
			<buildnumber file="bld/tetragon_build.number"/>
			<tstamp>
				<format property="build.date" pattern="dd-MMMM-yyyy HH:mm" unit="minute" locale="en"/>
			</tstamp>
			<echo message="*** ${ant.project.name} version: ${app.version} build #${build.number}, ${build.date} ***" level="info"/>
			<!-- <generateAppInfo buildnr="${build.number}" builddate="${build.date}" releasestage="${app.releasestage}"/> -->
		</sequential>
	</macrodef>
	
</project>
