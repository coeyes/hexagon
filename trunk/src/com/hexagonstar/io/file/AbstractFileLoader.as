/* * hexagon framework - Multi-Purpose ActionScript 3 Framework. * Copyright (C) 2007 Hexagon Star Softworks *       __    __ *    __/  \__/  \__    __ *   /  \__/HEXAGON \__/  \ *   \__/  \__/ FRAMEWORK_/ *            \__/  \__/ * * ``The contents of this file are subject to the Mozilla Public License * Version 1.1 (the "License"); you may not use this file except in * compliance with the License. You may obtain a copy of the License at * http://www.mozilla.org/MPL/ * * Software distributed under the License is distributed on an "AS IS" * basis, WITHOUT WARRANTY OF ANY KIND, either express or implied. See the * License for the specific language governing rights and limitations * under the License. */package com.hexagonstar.io.file{	import com.hexagonstar.data.types.Bit;	import com.hexagonstar.data.types.Byte;	import com.hexagonstar.env.exception.AbstractMethodException;	import com.hexagonstar.env.exec.AbstractTimeConsumer;	import com.hexagonstar.io.file.types.IFile;		import flash.events.Event;	import flash.events.EventDispatcher;	import flash.events.HTTPStatusEvent;	import flash.events.IOErrorEvent;	import flash.events.ProgressEvent;	import flash.events.SecurityErrorEvent;	import flash.net.URLRequest;	import flash.utils.getTimer;			/**	 * AbstractFileLoader provides common properties and methods for FileLoader	 * implementations. Extend this class to implement most methods required	 * for a concrete FileLoader.	 */	public class AbstractFileLoader extends AbstractTimeConsumer	{		////////////////////////////////////////////////////////////////////////////////////////		// Properties                                                                         //		////////////////////////////////////////////////////////////////////////////////////////				protected var _urlRequest:URLRequest;		protected var _filePath:String;		protected var _file:IFile;		protected var _isLoading:Boolean;		protected var _isAborted:Boolean;				////////////////////////////////////////////////////////////////////////////////////////		// Public Methods                                                                     //		////////////////////////////////////////////////////////////////////////////////////////				/**		 * Creates a new AbstractFileLoader instance.		 */		public function AbstractFileLoader(file:IFile)		{			super();			_file = file;			_isLoading = false;		}						/**		 * Loads the file that is at the specified path.		 * 		 * @param path Location of the file to load.		 */		public function load(path:String):void		{			_filePath = path;			_isStarted = true;			_isFinished = false;			_isAborted = false;			_startTime = getTimer();			_urlRequest = new URLRequest(_filePath);		}						/**		 * Cleans up Objects used by the FileReader.		 */		public function dispose():void		{			_file = null;			_urlRequest = null;		}						////////////////////////////////////////////////////////////////////////////////////////		// Getters & Setters                                                                  //		////////////////////////////////////////////////////////////////////////////////////////				/**		 * Returns the file path of the file that was requested to load.		 * 		 * @return location of the file to load.		 */		public function get filePath():String		{			return _filePath;		}						/**		 * Returns true if a file loading is in progress.		 */		public function get isLoading():Boolean		{			return _isLoading;		}						/**		 * Returns true if the file loading progress was aborted.		 */		public function get isAborted():Boolean		{			return _isAborted;		}						/**		 * Returns the pecentage of the file that is being loaded. This evaluates		 * the current percentage of the execution by using getBytesTotal and		 * getBytesLoaded. Returns -1 if the percentage is not evaluable, i.e. the		 * file has not started loading yet.		 * 		 * @return percentage of the file that is being loaded.		 */		override public function get percentage():int		{			if (isStarted || isFinished)			{				var p:int = (bytesLoaded.bytes / bytesTotal.bytes * 100);				if (p >= 100) p = 100;				return p;			}			else			{				return -1;			}		}						/**		 * Returns the current transfer rate for the execution. This evalutes		 * the transfer rate by using getBytesLoaded and getDuration.		 * 		 * @return transfer rate in bit (per second).		 */		public function get transferRate():Bit		{			return new Bit(bytesLoaded.bit / duration.inSeconds);		}						public virtual function get bytesTotal():Byte		{			throwAbstractMethodException();			return null;		}						public virtual function get bytesLoaded():Byte		{			throwAbstractMethodException();			return null;		}						public virtual function get fileContent():*		{			throwAbstractMethodException();			return null;		}						////////////////////////////////////////////////////////////////////////////////////////		// Event Handlers                                                                     //		////////////////////////////////////////////////////////////////////////////////////////				protected function onOpen(e:Event):void		{			dispatchEvent(new FileIOEvent(FileIOEvent.OPEN));		}				protected function onProgress(e:ProgressEvent):void		{			dispatchEvent(new FileIOEvent(FileIOEvent.PROGRESS));		}				protected function onHTTPStatus(e:HTTPStatusEvent):void		{			dispatchEvent(new FileIOEvent(FileIOEvent.HTTP_STATUS, null, "", e.status));		}				protected function onComplete(e:Event):void		{			removeEventListeners(e.target as EventDispatcher);			dispatchEvent(new FileIOEvent(FileIOEvent.COMPLETE));		}				protected function onIOError(e:IOErrorEvent):void		{			removeEventListeners(e.target as EventDispatcher);			dispatchEvent(new FileIOEvent(FileIOEvent.IO_ERROR, null, e.text));		}				protected function onSecurityError(e:SecurityErrorEvent):void		{			removeEventListeners(e.target as EventDispatcher);			dispatchEvent(new FileIOEvent(FileIOEvent.SECURITY_ERROR, null, e.text));					}						////////////////////////////////////////////////////////////////////////////////////////		// Private Methods                                                                    //		////////////////////////////////////////////////////////////////////////////////////////				/**		 * Adds standard event listeners for the specified file loader.		 * @private		 * 		 * @param loader the loader that should listen to the events.		 */		protected function addEventListeners(loader:EventDispatcher):void		{			loader.addEventListener(Event.OPEN, onOpen);			loader.addEventListener(ProgressEvent.PROGRESS, onProgress);			loader.addEventListener(Event.COMPLETE, onComplete);			loader.addEventListener(HTTPStatusEvent.HTTP_STATUS, onHTTPStatus);			loader.addEventListener(IOErrorEvent.IO_ERROR, onIOError);			loader.addEventListener(SecurityErrorEvent.SECURITY_ERROR, onSecurityError);			_isLoading = true;		}						/**		 * Removes the formerly added Event Listeners.		 * @private		 */		protected function removeEventListeners(loader:EventDispatcher):void		{			loader.removeEventListener(Event.OPEN, onOpen);			loader.removeEventListener(ProgressEvent.PROGRESS, onProgress);			loader.removeEventListener(Event.COMPLETE, onComplete);			loader.removeEventListener(HTTPStatusEvent.HTTP_STATUS, onHTTPStatus);			loader.removeEventListener(IOErrorEvent.IO_ERROR, onIOError);			loader.removeEventListener(SecurityErrorEvent.SECURITY_ERROR, onSecurityError);			_isLoading = false;		}						/**		 * throws an AbstractMethodException.		 * @private		 */		private function throwAbstractMethodException():void		{			throw new AbstractMethodException("Called an abstract method in AbstractFileLoader!");		}	}}